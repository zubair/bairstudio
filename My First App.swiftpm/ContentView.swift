import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "hammer.fill")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("My First App")
        }
    }
}
